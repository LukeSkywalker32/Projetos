// o laço "for" é usado para repetir um bloco de código várias vezes
// A sintaxe do laço "for" é a seguinte:
// for(inicialização; condição; incremento){}

for (let i = 0; i <= 10; i++) {
  console.log(i);
}

let users = ["João", "Maria", "José", "Ana"];

for (let i = 0; i < users.length; i++){ // o length é usado para saber o tamanho do array
    console.log(users[i]); //
}

